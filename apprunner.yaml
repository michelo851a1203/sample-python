version: 1.0
runtime: python3

build:
  commands:
    pre-build:
      - echo "\033[031m prebuild happening...\033[0m"
    build:
      - pip install --no-cache-dir -r requirements.txt

run:
  pre-run:
    - echo "pre run trigger here..."
  command: uvicorn main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080

